# Resources

Place Godot `.tres` and `.res` resources here.

This folder is a good future home for:

- stat resource templates
- item resource mirrors
- enemy resource definitions
- balance presets
- data adapters that read `/design` JSON
